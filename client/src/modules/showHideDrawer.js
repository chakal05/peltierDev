const state = {
    drawer: false,
    bars : false
};

const getters = {
    getDrawer : (state) => state.drawer,
    getBars : (state) => state.bars
};

const mutations = {};

const actions = {};

export default {
  state,
  getters,
  mutations,
  actions
};
